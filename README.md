# MirrorNet Demo
Compression-Aware Intelligence (CAI) states that hallucination is a representational conflict problem. Compression strain measures it. CAI is the framework for evaluating it. when two prompts with the same meaning produce different outputs, the model’s latent representations are in conflict and Compression-Aware Intelligence (CAI) measures that conflict as compression strain.
Run with:
```
pip install -r requirements.txt
streamlit run app.py
```
